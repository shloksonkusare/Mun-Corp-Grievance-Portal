{
  "name": "grievance-portal",
  "version": "1.0.0",
  "description": "WhatsApp-integrated geo-tagged grievance management system",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\" \"npm run ai\"",
    "dev:no-ai": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm install && npm run dev",
    "client": "cd client && npm install && npm run build && npm run dev",
    "ai": "cd ai_model && venv\\Scripts\\python -m uvicorn main:app --reload --port 8000",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
